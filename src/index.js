//import './main.css'; // -> webpack still process this as normal CSS
//import styles from './index.module.css'; // -> This will get processed as CSS Modules
const h1 = document.querySelector('h1');h1.classList.add(styles.h1);